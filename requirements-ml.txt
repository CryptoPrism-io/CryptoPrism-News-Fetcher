# Install base first: pip install -r requirements.txt
# Heavy ML deps — install separately to keep CI fast

# NLP (Tier 1)
transformers>=4.40.0
torch>=2.2.0
sentencepiece>=0.2.0

# Classical ML (Tier 2)
lightgbm>=4.3.0
scikit-learn>=1.4.0
shap>=0.45.0

# Deep learning time-series (Tier 3)
pytorch-forecasting>=1.1.0
neuralforecast>=1.7.0

# Utilities
tqdm>=4.66.0
pyarrow>=15.0.0
